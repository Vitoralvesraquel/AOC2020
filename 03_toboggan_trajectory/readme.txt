Instructions:

The terrain (input_terrain.txt) has open squares (.) and trees (#).
The same pattern repeats in the map to the right many times.
Determine the number of trees you would encounter if, for each of the following slopes (input_path.txt).
You start at the top-left corner and traverse the map all the way to the bottom.
